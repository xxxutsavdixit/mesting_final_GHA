name: This is yest
on:
  - push 
  - workflow_dispatch
jobs:
      echo-hello:
       runs-on: ubuntu-latest'
       steps:
           - name: rpint hello 
             run: echo "Hello"
      
      echo-goodbye:
       runs-on: ubuntu-latest'
       steps:
           - name: peint goodby 
             run: echo "GoodBy"


